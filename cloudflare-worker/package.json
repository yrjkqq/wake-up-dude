{
  "name": "wake-up-dude-worker",
  "version": "1.0.0",
  "description": "Rate-limited API Proxy for Wake Up Dude App",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240320.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@google/genai": "latest"
  }
}
